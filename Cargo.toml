[package]
name = "deoxidization"
version = "0.1.0"
edition = "2021"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[dependencies]
rand = "0.8.5"
bevy = { version = "0.10.1", features = ["dynamic_linking"] }
bevy_rapier2d = "0.21.0"

